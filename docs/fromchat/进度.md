# SANDO 整数学习交接（更新 2026-09-09）

给后续 agent：先读本文，再读 `docs/fromchat/SANDO_第二部分_阶段6至8_耗时归因与验证.md` 与 `docs/request-latency-v2/STATUS.md`。

## 一句话

基点 `ce09ee3` 上已 push `5c6f7b9`（联合单提议接线 + 可重建小表 + ADR-0007/0008 计时边界）。第二部分阶段6测量主线完成；阶段7/8 卡在多 T 重建与新模型身份。结果在 gitignore 的 `docker/dev-workspace/results/request-latency-v2/`。

## 硬约束

同前：不挂宿主源码进正式镜像做污染对照；冻结哈希与 AMPL 授权路径保持；性能改动与网络改动分开验证。

## Git（personal）

| Commit | 内容 |
|---|---|
| `ce09ee3` | 阶段 0–5 报告 |
| `5c6f7b9` | 单时间联合提议接线、可重建查询小表、usable/reclaim/publish、update 跳过冗余 compile |

分支 `feat/ampl-gurobi` → `personal/feat/ampl-gurobi`。

## 阶段状态（第二部分 6–8）

| 阶段 | 状态 |
|---|---|
| 6 计时/12重放/瓶颈/一改动 | **完成** |
| 6 扩至60 + 假设表 + 几何复核 | 60/假设完成；几何 AABB 笔记完成，圆柱复算阻塞 |
| 7 同预算四臂 | **部分**（固定时间 + 只学赋值）；只学时间/联合阻塞 |
| 8 补采清单 | **完成** |
| 8 查询器核验 + 新模型对照 | **阻塞**（缺观测字段 / 无新模型） |

## 下一步

1. `replay_integer_planning --candidate-limit 1` 重跑阶段7 matched 臂。
2. 第一部分补齐 `visible_map` 等可重建字段并发布新模型 sha256。
3. 第二部分复跑四臂与阶段8核验；可选 selected60 正式重放。
