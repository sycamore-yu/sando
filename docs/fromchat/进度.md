# SANDO 整数学习交接（2026-09-08 13:52 UTC）

给后续 agent：先读本文，再读 `docs/fromchat/改进.md`。Goal：完成改进.md 全阶段；每阶段验收后 commit+push 到 `personal/feat/ampl-gurobi`。

## 一句话

续采健康：seed33 重跑成功（retained=60，无 invalid；n100 不再 SIGSEGV），已到 seed34。阶段 0/1 验收已 push；2/3 代码已 push。采集结束后自动 waiter 会做小规模标注试点（`run_aligned_post_capture.py --wait-exit --limit-runs 3`）。

## 硬约束（违反即做错）

同前：不挂宿主源码进 `sando-integer-centered`；冻结哈希 `fd2c5e16…` / `75c50265…`；不第二套 Gazebo；采集结束前不正式 AMPL 标注；不并 excluded 数据。

## Git（已 push 到 personal）

| Commit | 内容 |
|---|---|
| `3332dca` | 阶段 0/1：sando-dev 工作流验收 |
| `c88bef4` | 阶段 2 代码：集合监督 + smoke |
| `cb67e90` | 阶段 3/4 代码：标注队列、采样、DAgger 聚合 |

分支 `feat/ampl-gurobi` → `personal/feat/ampl-gurobi`。工作树仅剩未跟踪：`.agents/` `.codex/` `AGENTS.md` `docs/fromchat/报告.md` `docs/fromchat/Untitled`。

## 现场采集

- 容器已 `docker restart`（清僵尸）。身份仍 `fd2c5e16…` / `75c50265…`。
- 失败原因：seed33 `unknown_dynamic-n100` exit **-11** → `invalid_capture_metadata` → campaign 返回 2 → 原 `run.sh` 的 `set -e` 中止全矩阵。旧 seed33 已隔离为 `train/seed33.invalid-segfault`。
- 续跑：`docker/results/integer-benchmark-aligned-plan/resume.sh`（遇单 seed 失败继续；seed 间 pkill gzserver）。日志追加到 `integer-benchmark-aligned-capture.log`；结束写 `integer-benchmark-aligned-capture.exit`。
- 写本文时：train seed33 完成且干净；正在 seed34 forest-hard。待做 train 35–39 + validation 100–109（约 2+ h）。
- 后台：`sando-dev` 上 `run_aligned_post_capture.py --wait-exit --limit-runs 3 --skip-train`（日志 `docker/dev-workspace/results/aligned_post_capture/waiter.log`）。**勿在采集结束前另开 AMPL 标注。**

监控：

```bash
docker top sando-integer-centered
tail -20 docker/results/integer-benchmark-aligned-capture.log
ls docker/results/integer-benchmark-aligned-capture.exit
ls docker/results/integer-benchmark-aligned-data/validation
tail -5 docker/dev-workspace/results/aligned_post_capture/waiter.log
```

## 阶段状态

| 阶段 | 状态 |
|---|---|
| 0 核验 | 完成 |
| 1 开发容器 | **验收完成** + push `3332dca` |
| 2 集合监督 | 代码+单测+合成 smoke 完成；A/B/C 真实数据未跑 |
| 3 吞吐/标注 | 队列代码完成；waiter 待 `.exit` 后试点 3 场标注 |
| 4 DAgger | 聚合脚本有；闭环未跑 |
| 5 评测报告 | 模板空 |

## 下一步

1. 等 `integer-benchmark-aligned-capture.exit`；核 train 0–39 + val 100–109。
2. Waiter 试点标注完成后：去掉 `--limit-runs` 全量标注（可 workers=1 再试 2），测吞吐。
3. A/B/C seed0 + 小拟合 → 阶段 2 实验 commit。
4. DAgger 两轮 → 阶段 4 commit。
5. 冻结评测填 `报告.md` → 阶段 5 commit。
6. 森林 `goal_reached` 超时：采集结束后单独诊断。
