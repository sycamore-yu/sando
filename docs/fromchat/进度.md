# SANDO 整数学习交接（2026-09-08 13:06 UTC）

给后续 agent：先读本文，再读 `docs/fromchat/改进.md` 和 `docs/adr/0005-dev-container-and-set-supervision.md`。不要凭旧日记推进。本文写完时采集仍在飞，数字以现场为准。

## 一句话

正式 `benchmark_aligned_v1` 串行采集还在 `sando-integer-centered` 上写（约 train seed33）；**阶段一开发容器已验收通过**；训练侧代码仍在宿主工作树**未提交**；**还没有学习收益结论**，也**没有**对正式数据做 AMPL 标注。

## 硬约束（违反即做错）

- 不要中断、重建、挂载源码进 `sando-integer-centered`。采集容器只挂 `docker/results → /results` 和 AMPL uuid。宿主脚本改动**不会**自动进该容器。
- 不要改冻结规划器。身份必须保持：
  - 源码清单 `/opt/sando-integer-source.json` SHA-256 `fd2c5e160174bf81e58a5597c652930c860a6ab697b9b47177d48c3d82a1977c`
  - 安装节点 `/root/sando_ws/install/sando/lib/sando/sando` SHA-256 `75c502654d845224af5a886268b605d9cdfe142aa3a3fbb48f4bde4fe599d786`
  - `sando_node.cpp` SHA-256 `64dd3585d7445a6b834a5f106d49b7229f7620d7cf13f65796e336200cfb10a0`
  - 镜像标签 `sando-integer-centered:20260907`（创建层 `sha256:b5ceb87b1968…`）
- 不要在同一 GPU 再开第二套 Gazebo 采集。`sando-integer-centered-w1/w2/w3` 是已排除的四路试验，只作负载诊断，数据不得并入训练集。
- 不要合并这些目录：`integer-benchmark-aligned-data`、`integer-racefix-serial-data`、`integer-centered-serial-data`、pilot 标签。
- 不要把 RViz-only 已知动态（`publish_trajs=True` 且规划器订 `/trajs`）放进主训练。
- 训练/验证才用 `train_box_v1` 起点随机化。测试和论文主评测仍是 `(0,0,2)→(105,0,2)`、yaw 0。
- 阶段一学的是给定时间系数下的**走廊赋值**；时间搜索、QP 连续优化、SANDO 安全规则保持原样（ADR 0001–0003）。
- 采集未完成前不要开正式标注/BC/代价/闭环/900 场评测。这不是卡死，是协议门。
- 采集仍在飞时不要切 Git 分支。用户要把历史协议做成代码分支，那是采集结束后的事。
- 不要把 SANDO 改成 YOPO 那种离线深度图+位姿倾倒。样本必须是规划器在环的规划实例。
- `sudo -n docker` 需要密码；当前用户可直接 `docker`。
- 没有可调用的 advisor 会话。ADR 0004 里的 “Advisor 判定”只是书面约定。

## 现场（写本文时重读）

| 项 | 值 |
|---|---|
| 宿主仓库 | `/home/tong/tongworkspace/dockerworkplace/sando` |
| 分支 / HEAD | `feat/ampl-gurobi` / `06d11edf0c5b04b61365e5a2eb4e0c6362713abf` |
| 跟踪远程 | `personal/feat/ampl-gurobi`（`https://github.com/sycamore-yu/sando.git`）。`origin` 是 `mit-acl/sando` |
| 工作树 | **脏**。过程改进**尚未 commit/push** |
| 正式容器 | `sando-integer-centered` Up，镜像 `sando-integer-centered:20260907` |
| 开发容器 | `sando-dev` Up，同镜像，挂宿主源码到 `/root/sando_ws/src/sando`，`ROS_DOMAIN_ID=91` |
| 开发二进制 | `docker/dev-workspace/install/sando/lib/sando/sando` SHA-256 `8b1717e2e09a38189c6a461d1c3ce73936d43d5d3a73982cc59d3bad451abc12`（≠ 正式 `75c50265…`） |
| 冻结指纹 | `docker/dev-workspace/freeze/latest.json`（含 `dev_sando` 与 `process_improvement_v1` 配置） |
| 正式采集协议 | `benchmark_aligned_v1`，计划 `docker/results/integer-benchmark-aligned-plan/plan.json`，`started_utc=2026-09-08T07:17:29Z` |
| 输出 | `docker/results/integer-benchmark-aligned-data/` |
| pause / exit 文件 | **都没有**。采集未暂停、未收尾 |

当前进程（`docker top sando-integer-centered` @ 13:06）：

- `capture_integer_learning_campaign.py --output .../train/seed33 --protocol benchmark_aligned_v1`
- `integer_learning_baseline_sim.py .../unknown_dynamic-n50-d0.65-seed33`
- 冻结 `.../install/sando/lib/sando/sando`（哈希仍是 `75c50265…`）

### 采集进度

- 训练 seed **0–32 完成**（33/40）。每 seed `retained=60`，合计约 **1980** 条实例（198 场）。
- seed33：正在飞 `unknown_dynamic-n50`。
- `validation/` **尚不存在**。还要训练 33–39 和验证 100–109。矩阵 50 seed × 6 场 = 300 场；完成约 33×6 ≈ 198/300。
- 墙钟节奏约 **9 min/seed**。剩余约 17 seed（train 7 + val 10）≈ **2.5–3 h**（以现场为准）。
- 吞吐快照 `docker/dev-workspace/results/aligned_throughput.json`：198 场、1980 实例、飞行小时 4.56、**435 实例/飞行小时**。这是采集保留量，**不是已验收标签**。

飞行 `success` 不要当成“没飞”：

| 族 | 只读诊断（至 seed29 附近） | 说明 |
|---|---|---|
| unknown_dynamic | 多数 `success=True` | 偶发失败单独看 |
| static_forest | **全部** `success=False`，原因一律 `observation timeout: goal_reached was not received` | 到 seed29：93 场中 **83** 场 `goal_distance_m < 1m`（中位 ~0.27m），**10** 场仍远（>5m）。实例仍保留。**现在不要改正在飞的观测逻辑** |

## 本会话完成了什么

对照 `docs/fromchat/改进.md` 的阶段。

### 阶段零（只读核验）— 完成

路径对应已核对；正式写入者仍是冻结节点 + 容器内脚本，不是当前宿主 Git 树。

### 阶段一（开发容器）— **验收完成**

入口：

```bash
make -C docker dev-up|dev-build|dev-test|dev-shell|dev-status|dev-down|dev-freeze
# 或 ./docker/dev.sh up|build|test|shell|status|down|freeze|smoke|probe
```

已核验：

- 双向 bind mount（`./docker/dev.sh smoke`）
- AMPL/Gurobi 探针 `ampl_gurobi_probe_ok x=1.0`（Gurobi 13.0.2）
- 第一次完整 `colcon` 完成；`dev_sando` 存在且哈希 ≠ 正式
- 增量编译：touch `tests/ampl_model/test_instance_reservoir.cpp` 后重编 ~32s（对象 mtime 12:49）；无改动再编 ~8.5s
- `make -C docker dev-test`：**13/13 ctest 通过**（含 `ampl_adapter_real_model`、`integer_set_loss` 等）+ Python 过程改进测试
- `ensure_python`：CPU torch 2.14.0 在 `docker/dev-workspace/python`；venv 必须 `--system-site-packages`（否则盖住系统 `python3` 后缺 PyYAML）
- `./docker/dev.sh freeze` 已写 `docker/dev-workspace/freeze/latest.json`

脚本坑（已修）：

- `docker/dev.sh` 不要在双引号 `docker exec bash -lc` 里嵌套 `<<'PY'`
- 不要用 bash 4 的 `${var@Q}`；不要传失效的 `--log-base`
- `docker exec` 一律 `source docker/dev_env.sh`
- 重建 venv 时**不能** `rm -rf` 挂载点本身，只能清内容 + `venv --system-site-packages`

### 阶段二（集合监督）— 代码完成，无真实训练结果

- `scripts/integer_set_supervision.py` + `train_integer_corridor_policy.py --method set`
- 本会话发现并修复：`capture_integer_learning_campaign.py` 与 `train_integer_corridor_policy.py` 曾被截断写入工具输出垃圾；已从 HEAD 恢复并重新打上容量/采样与集合监督补丁
- `test_capture_campaign.py` 容量用例不再误用 aligned 的 `train_box_v1` mock 断言
- 真实 8–16 条拟合和 A/B/C 仍等完整代价表

### 阶段三（吞吐流水线）— 代码完成，未对正式数据求解

标注队列/采样器/测吞吐脚本仍在。采集期间不抢 AMPL。双物理容器未做。

### 阶段四 / 五 — 未运行

无学生模型、无完整代价表、无 900 场。`docs/fromchat/报告.md` 仍是模板。

过程改进判定：`docs/process-improvement-v1-report.md` — 目标 1 **部分支持→更强**（开发二进制与测试已通）；目标 2、3 仍未支持或反对。

## Git 脏清单（接手后先 `git status`）

相对上一交接，额外注意：

- `docker/dev.sh`（venv `--system-site-packages`、清挂载内容）
- `scripts/capture_integer_learning_campaign.py`、`scripts/train_integer_corridor_policy.py`（截断修复 + 补丁重打）
- `tests/ampl_model/test_capture_campaign.py`（容量 mock）

未跟踪学习代码同前：`docker/dev_*.sh|py`、`scripts/integer_*`、`docs/fromchat/`、`docs/adr/0005-*` 等。不要提交 `.agents/` `.codex/`、`docker/results/`、`docker/dev-workspace/`（已在 `.gitignore`）。

未再被要求 commit 时不要擅自提交。

## 下一步（按顺序）

1. **只监控正式采集。**  
   `docker top sando-integer-centered`；train 到 seed39；再看 `validation/seed100` 起。出现 `docker/results/integer-benchmark-aligned-capture.exit` 后核 50 个 seed 的 `output.json` 和哈希。
2. 采集全部结束后：用 `integer_label_queue.py` 对 **aligned 完成快照副本** 标注；不要原地改采集目录。先小规模核残差 2e-6。
3. 森林 `goal_reached` 超时：采集结束后单独开诊断（多数已到目标附近却未收到 topic）。改判据属于采集脚本，正式规划器哈希不能动。
4. 有完整表后再跑 A/B/C 和一个种子的小拟合；DAgger / 等量续训 / 冻结评测按改进文档阶段四、五。
5. 可选且仅在采集结束后：commit 过程改进到 `personal/feat/ampl-gurobi`；停空闲容器；默认协议改 aligned。

## 常用命令

```bash
# 现场
docker top sando-integer-centered
docker exec sando-integer-centered sha256sum /opt/sando-integer-source.json /root/sando_ws/install/sando/lib/sando/sando
ls docker/results/integer-benchmark-aligned-data/train
ls docker/results/integer-benchmark-aligned-data/validation
python3 scripts/measure_integer_throughput.py \
  --input docker/results/integer-benchmark-aligned-data \
  --output docker/dev-workspace/results/aligned_throughput.json

# 开发（采集期间 BUILD_JOBS=1）
make -C docker dev-status
BUILD_JOBS=1 make -C docker dev-build
make -C docker dev-test
./docker/dev.sh smoke && ./docker/dev.sh probe
```

## 不要做的“加速”

- 不要为了赶吞吐改成 YOPO 离线射线。
- 不要复用 excluded 四路 worker 数据。
- 不要在正式采集还占 GPU 时再开第二套 Gazebo。
- 不要声称 435 实例/小时已经等于标注吞吐或学习改进。
- 不要在采集期间开 AMPL 标注抢许可证。
