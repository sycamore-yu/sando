---
status: accepted
---

# 开发容器与集合监督进入主线，正式采集保持冻结

正式未知动态采集继续使用冻结镜像和规划器哈希，不把宿主机开发树挂进采集容器。新增长驻开发容器 `sando-dev` 绑定宿主机源码到 `/root/sando_ws/src/sando`，产物写到独立的 `docker/dev-workspace/`。训练增加完整赋值集合损失；近优容差固定为相对 1%、尺度 1、绝对 2e-6。标注队列只读取已完成快照并复制后再求解。开发采样上限 20 不改变正式首轮 10 / 补采 5。
